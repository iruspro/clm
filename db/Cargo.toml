[package]
name = "db"
version = "0.1.0"
edition = "2024"

[features]
# Test-only helpers (`db::testing`), enabled from a consumer's dev-dependencies.
testing = []

[dependencies]
rusqlite = { workspace = true }
sea-query = { workspace = true }
